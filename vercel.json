{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "rm -rf public && mkdir public && cp index.html app.js styles.css public/ && cp -R data public/data",
  "outputDirectory": "public",
  "functions": {
    "api/live.mjs": {
      "maxDuration": 60
    }
  }
}
